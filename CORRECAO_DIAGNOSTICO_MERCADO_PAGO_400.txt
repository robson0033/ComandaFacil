CORREÇÃO DO DIAGNÓSTICO DE ERRO HTTP 400 DO MERCADO PAGO

Alterações:
- a função mp() agora lê o corpo bruto da resposta antes de tratar o erro;
- respostas JSON e texto são preservadas de forma sanitizada;
- erros HTTP agora carregam httpStatus, status, endpointPath, providerCode,
  providerMessage, providerCauses e responseReceived=true;
- o log "Pix do pedido" passa a revelar a causa real enviada pelo provedor,
  sem registrar token, Authorization, QR Code ou payload completo;
- criado teste test/mercadoPagoProviderErrorDetails.test.js.

Verificações:
- node --check src/controllers/pagamentoController.js: aprovado;
- node --check src/middleware/mercadoPagoSecurity.js: aprovado;
- teste novo: 3 aprovados, 0 falhas;
- a suíte mercadoPagoP0.test.js não pôde ser executada neste ambiente por falta
  da dependência express-session, que não está instalada no ZIP extraído.

Próximo passo após deploy:
- refazer um pedido Pix;
- copiar o novo log "Pix do pedido";
- verificar providerCode, providerMessage e providerCauses;
- não remover application_fee antes de conhecer a resposta real do Mercado Pago.

Nenhum banco foi acessado. Nenhum índice foi aplicado. Não houve deploy, commit ou push.
